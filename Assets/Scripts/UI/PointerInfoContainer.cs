using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PointerInfoContainer : MonoBehaviour
{
    private Vector3 targetPosition;
    public Vector3 TargetPos { get => targetPosition; set => targetPosition = value; }
}
